/**
 * Phase 6 — HTTP actions for Flask bridge (e.g. usage.verifyAndIncrement).
 * https://docs.convex.dev/functions/http-actions
 */
import { httpRouter } from "convex/server";

const http = httpRouter();

export default http;
