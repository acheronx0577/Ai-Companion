[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[phases.setup]
nixPkgs = ["python312"]

# Faster Railway builds: Groq + OAuth only (see requirements-railway.txt)
[phases.install]
cmds = ["python -m pip install -r requirements-railway.txt"]
